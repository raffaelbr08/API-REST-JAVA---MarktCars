API REST PROJETO MARKETCARS
WADL para o Postman: http://localhost:8080/marketcars/application.wadl

No body da chamada POST login mandar os dados em formato JSON:

{
    "usuario":"admin",
    "senha": "admin"
}
